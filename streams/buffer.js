//buffer = Espaço de memória para armazenar dados
//buffer = usado para transitar dados de um lugar para outro de maneira rapida
//buffer = maneira de ler e gravar dados na memoria de maneira binaria

const buf = Buffer.from("Esta assistindo oque")

console.log(buf)
//console.log(buf.toJSON())